package com.example.solobeast.ui;

import androidx.appcompat.app.AppCompatActivity;

import android.os.Bundle;
import android.widget.Switch;

import com.example.solobeast.R;

public class SettingsAct extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
//        setContentView(R.layout.activity_settings);


//        Switch notificationSwitch = findViewById(R.id.notification_switch);
//        Switch soundSwitch = findViewById(R.id.sound_switch);
//        Switch darkModeSwitch = findViewById(R.id.dark_mode_switch);
//        Switch fontSizeSwitch = findViewById(R.id.font_size_switch);

        // Set listeners for the switch preferences
//        notificationSwitch.setOnCheckedChangeListener((buttonView, isChecked) -> {
//            // Handle notification switch preference changes
//            if (isChecked) {
//                // Enable notifications
//            } else {
//                // Disable notifications
//            }
//        });
//
//        soundSwitch.setOnCheckedChangeListener((buttonView, isChecked) -> {
//            // Handle sound switch preference changes
//            if (isChecked) {
//                // Enable sound
//            } else {
//                // Disable sound
//            }
//        });
//
//        darkModeSwitch.setOnCheckedChangeListener((buttonView, isChecked) -> {
//            // Handle dark mode switch preference changes
//            if (isChecked) {
//                // Enable dark mode
//            } else {
//                // Disable dark mode
//            }
//        });
//
//        fontSizeSwitch.setOnCheckedChangeListener((buttonView, isChecked) -> {
//            // Handle font size switch preference changes
//            if (isChecked) {
//                // Adjust font size
//            } else {
//                // Reset font size to default
//            }
//        });
    }

}